# PI4-THERMO — Code

Repo code du projet PI4 (désagrégation TCL).
Les documents (rapports, figures, données lourdes) sont sur OneDrive (pas ici).

## Structure (provisoire)
- src/ : code Python
- tests/ : tests unitaires
- notebooks/ : explorations Jupyter
- scripts/ : scripts utilitaires

## Environnement
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

## Données
Ne pas committer de données lourdes (cf. OneDrive).
